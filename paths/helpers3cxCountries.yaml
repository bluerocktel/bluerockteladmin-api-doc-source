  /v1/helpers/3cxCountries:
    get:
      summary: Returns the 3cx countries list.
      description: Returns the 3cx countries list.
      operationId: get-3cx-countries
      tags:
        - 3cx
      responses:
        '200':
          description: a JSON array of continents and countries
          content:
            application/json:
              schema:
                type: array
